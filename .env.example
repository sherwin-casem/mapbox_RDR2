# Copy to .env.local and set your public Mapbox token (Vite exposes only VITE_* to the client).
VITE_MAPBOX_TOKEN=
# Optional override (default: Mapbox Dark v11)
# VITE_MAPBOX_STYLE=mapbox://styles/mapbox/dark-v11
